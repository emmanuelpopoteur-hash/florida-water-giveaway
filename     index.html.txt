<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Florida Family Water Giveaway | Ecoverse</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary-yellow: #FFD700;
            --dark-black: #121212;
            --bg-gray: #1a1a1a;
            --white: #ffffff;
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background: var(--dark-black);
            color: var(--white);
            overflow-x: hidden;
        }

        .container {
            max-width: 500px;
            margin: auto;
            padding: 20px;
        }

        header {
            text-align: center;
            padding-top: 40px;
        }

        .badge {
            display: inline-block;
            border: 2px solid var(--primary-yellow);
            color: var(--primary-yellow);
            padding: 8px 18px;
            border-radius: 50px;
            font-weight: 700;
            margin-bottom: 20px;
            font-size: 0.9rem;
        }

        h1 {
            font-size: 3rem;
            font-weight: 900;
            line-height: 1.05;
            margin-bottom: 20px;
        }

        .subheadline {
            color: #bdbdbd;
            font-size: 1.2rem;
            margin-bottom: 40px;
        }

        .benefits-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 18px;
            margin-bottom: 40px;
        }

        .benefit-card {
            background: var(--bg-gray);
            border-radius: 22px;
            padding: 35px 20px;
            text-align: center;
            border: 1px solid #2d2d2d;
            transition: var(--transition);
        }

        .benefit-card:hover {
            border-color: var(--primary-yellow);
            transform: translateY(-3px);
        }

        .icon {
            font-size: 2rem;
            display: block;
            margin-bottom: 15px;
        }

        .benefit-text {
            font-weight: 700;
            font-size: 1.1rem;
        }

        .form-container {
            background: white;
            color: black;
            border-radius: 30px;
            padding: 35px 25px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
            margin-bottom: 30px;
        }

        .form-group {
            margin-bottom: 24px;
        }

        label {
            display: block;
            font-weight: 700;
            margin-bottom: 10px;
            font-size: 1rem;
            color: #444;
        }

        input,
        select {
            width: 100%;
            padding: 18px;
            border-radius: 16px;
            border: 2px solid #ececec;
            font-size: 1.1rem;
            outline: none;
            transition: var(--transition);
        }

        input:focus,
        select:focus {
            border-color: var(--primary-yellow);
        }

        .cta-button {
            width: 100%;
            background: var(--primary-yellow);
            border: none;
            padding: 22px;
            border-radius: 18px;
            font-size: 1.6rem;
            font-weight: 900;
            cursor: pointer;
            transition: var(--transition);
            color: black;
            margin-top: 10px;
        }

        .cta-button:hover {
            transform: scale(1.02);
        }

        .trust-text {
            text-align: center;
            margin-top: 18px;
            color: #888;
            font-size: 0.95rem;
        }

        footer {
            text-align: center;
            color: #777;
            font-size: 0.85rem;
            line-height: 1.7;
            padding-bottom: 40px;
        }

        .success-message {
            text-align: center;
            padding: 40px 10px;
        }

        .success-message h2 {
            font-size: 3rem;
            margin-bottom: 15px;
        }

        .success-message p {
            color: #666;
            font-size: 1.2rem;
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 2.5rem;
            }

            .benefit-text {
                font-size: 1rem;
            }

            .cta-button {
                font-size: 1.4rem;
            }
        }
    </style>
</head>

<body>

<div class="container">

    <header>
        <div class="badge">LIMITED TIME OFFER</div>

        <h1>
            🎁 Florida Family
            Water Giveaway
        </h1>

        <p class="subheadline">
            Win a FREE Reverse Osmosis System for Your Home
        </p>
    </header>

    <div class="benefits-grid">

        <div class="benefit-card">
            <span class="icon">💧</span>
            <div class="benefit-text">Free Water Testing</div>
        </div>

        <div class="benefit-card">
            <span class="icon">📏</span>
            <div class="benefit-text">TDS Meters</div>
        </div>

        <div class="benefit-card">
            <span class="icon">🎁</span>
            <div class="benefit-text">Quality Tools</div>
        </div>

        <div class="benefit-card">
            <span class="icon">🏠</span>
            <div class="benefit-text">FL Homeowners</div>
        </div>

    </div>

    <div class="form-container">

        <form 
            name="giveaway"
            method="POST"
            data-netlify="true"
            netlify
        >

            <input type="hidden" name="form-name" value="giveaway">

            <div class="form-group">
                <label>Full Name</label>

                <input
                    type="text"
                    id="fullName"
                    name="fullName"
                    placeholder="John Doe"
                    required
                >
            </div>

            <div class="form-group">
                <label>Phone Number</label>

                <input
                    type="tel"
                    id="phone"
                    name="phone"
                    placeholder="(407) 555-0123"
                    required
                >
            </div>

            <div class="form-group">
                <label>ZIP Code</label>

                <input
                    type="text"
                    id="zip"
                    name="zip"
                    placeholder="34769"
                    required
                    pattern="[0-9]{5}"
                >
            </div>

            <div class="form-group">
                <label>Are you a Homeowner?</label>

                <select
                    id="homeowner"
                    name="homeowner"
                    required
                >
                    <option value="" disabled selected>
                        Select option
                    </option>

                    <option value="yes">
                        Yes, I own my home
                    </option>

                    <option value="no">
                        No, I am renting
                    </option>
                </select>
            </div>

            <button type="submit" class="cta-button">
                ENTER GIVEAWAY
            </button>

            <div class="trust-text">
                ✅ Your data is secure and encrypted.
            </div>

        </form>

    </div>

    <footer>
        No purchase necessary.
        Florida residents only.
        Winners will be notified via phone.
        This promotion is not affiliated with Facebook or Instagram.
    </footer>

</div>

</body>
</html>